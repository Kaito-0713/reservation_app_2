<h2 class="page-title">マイページ</h2>

<div class="profile-card">
  <div class="profile-header">
    <div class="profile-image">
      <% if current_user.icon_image.attached? %>
        <%= image_tag current_user.icon_image.variant(resize_to_limit: [150, 150]), class: "circle-img" %>
      <% else %>
        <%= image_tag "icon.png", size: "150x150", class: "circle-img" %>
      <% end %>
    </div>

    <div class="profile-info">
      <p><strong>名前:</strong> <%= current_user.name %></p>
      <p><strong>メールアドレス:</strong> <%= current_user.email %></p>
      <p><strong>自己紹介:</strong> <%= current_user.profile.presence || "未設定" %></p>
    </div>
  </div>

  <div class="profile-actions">
    <%= link_to "プロフィール編集", edit_user_registration_path, class: "btn" %>
  </div>
</div>

<hr class="divider">

<div class="links">
  <h3>各ページへのリンク</h3>
  <ul>
    <li><%= link_to "施設一覧を見る", facilities_path %></li>
    <li><%= link_to "予約一覧を見る", reservations_path %></li>
  </ul>
</div>

