<h1>検索結果</h1>

<p>該当件数: <%= @rooms.size || 0 %> 件</p>

<table>
  <thead>
    <tr>
      <th>施設名</th>
      <th>住所</th>
      <th>施設詳細</th>
      <th>宿泊料金(/日)</th>
    </tr>
  </thead>
  <tbody>
    <% @rooms.each do |room| %>
      <tr>
        <td><%= link_to room.name, room_path(room) %></td>
        <td><%= room.address %></td>
        <td><%= truncate(room.detail, length: 80) %></td>
        <td><%= number_to_currency(room.price, unit: "¥", precision: 0) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<div style="margin-top:12px;">
  <%= link_to "一覧へ戻る", rooms_path %>
</div>
